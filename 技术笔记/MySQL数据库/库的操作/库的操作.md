# 库的操作

## 创建数据库

```mysql
CREATE DATABASE [IF NOT EXISTS] db_name [create_specification [,
create_specification] ...]

create_specification:
[DEFAULT] CHARACTER SET charset_name
[DEFAULT] COLLATE collation_name
```
说明：
- 大写的表示关键字
- [] 是可选项
- CHARACTER SET：指定数据库采用的字符集
- COLLATE：指定数据库字符集的校验规则


## 创建数据库案例

- 创建数据库
```mysql
create database [you_file];
```
>说明：当我们创建数据库没有指定字符集和校验规则时，系统使用默认的字符集：utf8，校验规则是：utf8_general_ci

- 创建一个使用 utf8 字符集的数据库
```mysql
create database [you_file] charset=utf8;
```

- 创建一个使用 utf 字符集，并带校对规则的数据库
```mysql
create database [you_file] collate utf8_general_ci;
```

## 字符集和校验规则

创建数据库是，有两个编码集：
1. 数据库编码集（每一个连接的数据库必须统一编码集）
2. 数据库校验集

### 查看系统默认字符集及校验规则



### 查看数据库支持的字符集

### 查看数据库支持的字符集校验规则

### 校验规则对数据库的影响
- 不区分大小写
- 区分大小写

#### 进行查询

##### 不区分大小写
- 不区分大小写的查询及结果
- 不区分大小写排序及结果

##### 区分大小写
- 区别大小写的查询及结果
- 区别大小写排序及结果

## 操纵数据库

### 查看数据库

### 显示创建语句

### 修改数据库

### 删除数据库

### 备份和恢复

#### 备份

#### 还原

#### 注意事项

### 查看连接情况