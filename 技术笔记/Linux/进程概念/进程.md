## 进程的概念

### 进程形成与销毁过程

1. 执行 `.c` 文件，先存磁盘中
2. 需要使用时，将 `.c` 形成的**可执行代码和数据**放入内存里
3. 操作系统在内存中找到**可执行代码和数据**，并将其链接起来
![576](图片/进程形成过程.png)
>总结：为了方便管理进程，先描述再组织，将进程管理转换成对链表的增删查改
>
>进程 = 内核数据结构对象 + 自己的代码和数据
>进程 = PCB(task_struct) + 自己的代码和数据

```c
// struct XXX -> 操作系统(PCB) -> struct task_struct{}
struct XXX
{
	// 代码地址
	// 数据地址
	// id
	// 优先级
	// 状态
	...
	struct XXX *next; // 将每一个进程链接起来，形成链表，方便增删查改
}
```

CPU 拿到进程去调度，找到对应的 PCB，通过 PCB 找到对应的的代码和数据。进程运行结束，在操作系统内将代码和数据释放掉，链表将结束的进程释放掉，此时操作系统完成了对进程的删除

### PCB(task_struct)

#### 内容分类

- 标示符：描述本进程的唯一标示符，用来区别其他进程。
- 状态：任务状态，退出代码，退出信号等。
- 优先级：相对于其他进程的优先级。
- 程序计数器：程序中即将被执行的下一条指令的地址。
- 内存指针：包括程序代码和进程相关数据的指针，还有和其他进程共享的内存块的指针
- 上下文数据：进程执行时处理器的寄存器中的数据。
- 1/O状态信息：包括显示的I/O请求，分配给进程的I/O设备和被进程使用的文件列表。
- 记账信息：可能包括处理器时间总和，使用的时钟数总和，时间限制，记账号等。
- 其他信息

#### 组织进程

在操作系统中，所有进程的 PCB 用双链表链接起来

```
 —— —— —— —— —— —— —— —— —— —— —
|                               |
-> next -> next -> next -> next -
- prev <- prev <- prev <- prev <-
|                               |
 —— —— —— —— —— —— —— —— —— —— —
```

> 怎么理解进程放在双链表里 又 属于队列？
1. 原始双链表：下一个节点指向结构体的开头（从上到下字节依次增大）
2. Linux上的双链表：下一个节点的 list 指向下一个节点的 list

> 问题：Linux上的双链表，怎么去访问结构体里面的任何一个成员

在 `struct task_struct a`中，`&a == &a.x`，由于结构体中的数据从上到下字节依次增大，就可以用
- **list 强转为0 -> list 地址 = 偏移量**（ `&((struct task_struct*)0 -> list)` ）
- **结构体地址里 list 地址 - 偏移量 = 结构体头数据**（`list - &((struct task_struct*)0 -> list)` ）
 ![](图片/Linux内核双链表.png)
### 准备工作

#### Makefile 与 myprocess

**Makefile**
```bash
myprocess:myprocess.c
	gcc -o $@ $^
.PHONY:clean
clean:
	rm -f myprocess
```

**myprocess.c**
```c
#include <stdio.h>
#include <unistd.h>       // sleep

int main()
{
	while(1)
	{
		printf("我是一个进程\n");
		sleep(1);
	}
	
    return 0;
}
```

#### 进程指令

- 展示所有进程：
	- `ps axj`
	- `ls /proc` 文件形式
- 只看启动进程
	- `ps axj | grep [file]`
- 只看头进程
	- `ps ajx | head -1;ps axj | grep [file]`
	- `ps ajx | head -1 && ps axj | grep [file]`
	- 不包含grep： `ps ajx | head -1 && ps axj | grep [file] | grep -v grep`
- 展示一个进程的所有信息
	- `ls /proc/[id] -l`
- 杀掉进程
	- `Ctrl + c`
	- `kill -9 [pid]`

我们运行的所有命令，全都是进程

### 获取进程标识符

- 进程id：pid
- 父进程id：ppid
```c
#include <stdio.h>
#include <unistd.h>       // sleep
#include <sys/types.h>    // 进程id

int main()
{
    while(1)
    {
	    printf("进程开始运行,pid=%d\n", getpid());
	    sleep(1);
    }
    
    return 0;
}
```

```
此时循环打印

我是一个进程~, 我的pid=10261, 我的父进程id=10215
```

我们再打开另外一个 Xshall 查看 `myprocess` 运行的进程，使用两个命令查看：
1. `ps axj | head -1 && ps axj | grep [file]`
2. `ls /proc/[file id] -l`
```bash
[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ps axj | head -1 && ps axj | grep myprocess
 PPID   PID  PGID   SID TTY      TPGID STAT   UID   TIME COMMAND
10215 10261 10261 10215 pts/3    10261 S+    1001   0:00 ./myprocess
10239 10269 10268 10239 pts/4    10268 S+    1001   0:00 grep --color=auto myprocess

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ls /proc
1      10214  15195  22     32   450   66    941        driver       key-users   net           sysrq-trigger
10     10215  15206  23     327  453   690   acpi       execdomains  kmsg        pagetypeinfo  sysvipc
10077  10236  15353  239    33   456   7     buddyinfo  fb           kpagecount  partitions    timer_list
10079  10238  17     24     34   457   751   bus        filesystems  kpageflags  sched_debug   timer_stats
10080  10239  172    254    352  462   7516  cgroups    fs           loadavg     schedstat     tty
10109  10261  18     259    353  4636  756   cmdline    interrupts   locks       scsi          uptime
10111  10280  19     260    375  466   757   consoles   iomem        mdstat      self          version
10112  11     1999   3      42   47    766   cpuinfo    ioports      meminfo     slabinfo      vmallocinfo
10164  12     2      30122  426  494   794   crypto     irq          misc        softirqs      vmstat
10166  13     20     30129  43   495   8     devices    kallsyms     modules     stat          zoneinfo
10167  15     2021   30175  44   5     9     diskstats  kcore        mounts      swaps
10212  15188  21     31     45   6251  9148  dma        keys         mtrr        sys

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ls /proc/10261 -l
lrwxrwxrwx 1 zjh zjh 0 Mar 24 19:08 cwd -> /home/zjh/lesson12             # 可执行程序所在路径
lrwxrwxrwx 1 zjh zjh 0 Mar 24 19:08 exe -> /home/zjh/lesson12/myprocess   # 进程文件
```

#### 为什么父进程的 id 不变

获取父进程的标识符，查看父进程的信息
```bash
[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ./myprocess
我是一个进程~, 我的pid=10735, 我的父进程id=10296
我是一个进程~, 我的pid=10735, 我的父进程id=10296
我是一个进程~, 我的pid=10735, 我的父进程id=10296

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ps axj | head -1 && ps axj | grep 10296
 PPID   PID  PGID   SID TTY      TPGID STAT   UID   TIME COMMAND
10295 10296 10296 10296 pts/3    10735 Ss    1001   0:00 -bash
10296 10735 10735 10296 pts/3    10735 S+    1001   0:00 ./myprocess
10239 10743 10742 10239 pts/4    10742 S+    1001   0:00 grep --color=auto 10296
```
可以看到父进程是个 `bash`，也就是每一个用户登录，就增加一个 `bash`，每一个命令的父进程都是 `bash`（用户）

### 杀死进程

杀死进程有两个指令：
1. `kill -9 标号` （需在另一台 Xshall）
2. `Ctrl + c`
```bash
# 需要用另外一个 Xshall 执行
# 也可以用 Ctrl + c 杀死进程
[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ kill -9 10317

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ./myprocess
我是一个进程~, 我的pid=10317, 我的父进程id=10296
我是一个进程~, 我的pid=10317, 我的父进程id=10296
我是一个进程~, 我的pid=10317, 我的父进程id=10296
我是一个进程~, 我的pid=10317, 我的父进程id=10296
Killed # 进程已被杀死
```

### 删除文件对进程有无影响？

```bash
[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ll
total 20
-rw-rw-r-- 1 zjh zjh   73 Mar 18 18:52 Makefile
-rwxrwxr-x 1 zjh zjh 8720 Mar 24 11:20 myprocess
-rw-rw-r-- 1 zjh zjh  235 Mar 24 11:20 myprocess.c

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ rm -rf myprocess

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ls /proc/10321 -l
lrwxrwxrwx 1 zjh zjh 0 Mar 24 19:18 cwd -> /home/zjh/lesson12                 # 进程所在路径
lrwxrwxrwx 1 zjh zjh 0 Mar 24 19:18 exe -> /home/zjh/lesson12/myprocess (deleted)   # 文件被删除
```

当我们删除文件时，进程还在进行中（因为代码和数据已经拷贝在内存中运行，已有进程，因此删除源文件对进程运行无影响）

### 进程所在路径

#### fopen

进程在启动时，会记录自己的所在路径，当执行 `fopen("XXX.txt", "w");` 时，`fopen` 会获得当前路径，把写入的文件拼到当前路径的后面。比如我们用 `fopen` 写入 `xxx.txt` 文件，进程所在路径为 `/home/zjh/lesson12`，拼接后文件就在此路径创建出来：`/home/zjh/lesson12/XXX.txt`
```bash
[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ cat myprocess.c
#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main()
{
    fopen("XXX.txt", "a");  # 创建文件
    while(1)
    {
        sleep(1);
        printf("我是一个进程~, 我的pid=%d, 我的父进程id=%d\n", getpid(), getppid());
    }

    return 0;
}

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ll
total 20
-rw-rw-r-- 1 zjh zjh   73 Mar 18 18:52 Makefile
-rwxrwxr-x 1 zjh zjh 8768 Mar 24 19:40 myprocess
-rw-rw-r-- 1 zjh zjh  261 Mar 24 19:40 myprocess.c

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ./myprocess
我是一个进程~, 我的pid=10535, 我的父进程id=10296
^C

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ll
total 20
-rw-rw-r-- 1 zjh zjh   73 Mar 18 18:52 Makefile
-rwxrwxr-x 1 zjh zjh 8768 Mar 24 19:40 myprocess
-rw-rw-r-- 1 zjh zjh  261 Mar 24 19:40 myprocess.c
-rw-rw-r-- 1 zjh zjh    0 Mar 24 19:40 XXX.txt # 创建文件
```

#### 修改进程所在路径

需要用到 `chdir函数` 来进行对路程的路径切换
```bash
[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ cat myprocess.c
#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main()
{
    chdir("/home/zjh");     # 修改路径
    fopen("XXX.txt", "a");  # 创建文件
    while(1)
    {
        sleep(1);
        printf("我是一个进程~, 我的pid=%d, 我的父进程id=%d\n", getpid(), getppid());
    }

    return 0;
}

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ make
gcc -o myprocess myprocess.c

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ./myprocess
我是一个进程~, 我的pid=10712, 我的父进程id=10296
^C

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ls /home/zjh
code.c  gitcode  hello.txt  install.sh  lesson10  lesson11  lesson12  lesson15  lesson9  lib  XXX.txt # 创建文件
```

### 创建进程

#### 子进程

需要用到 `fork()` 函数来创建子进程
```c
[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ cat myprocess.c
#include <stdio.h>
#include <unistd.h>       // sleep
#include <sys/types.h>    // getpid子进程 getppid父进程

int main()
{
    printf("父进程开始运行,pid=%d\n", getpid());
    fork();
    printf("进程开始运行,pid=%d\n", getpid());
    
    return 0;
}

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ make
gcc -o myprocess myprocess.c

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ./myprocess
父进程开始运行,pid=12531
进程开始运行,pid=12531
进程开始运行,pid=12532
```
子进程没用自己的代码和数据，所以子进程需要访问父进程的数据和代码，所以打印了 `pid=12532`

#### 子程序返回值

```bash
[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ cat myprocess.c
#include <stdio.h>
#include <unistd.h>       // sleep
#include <sys/types.h>    // getpid子进程 getppid父进程

int main()
{
    printf("父进程开始运行,pid=%d\n", getpid());
    pid_t id = fork();
    if(id < 0)
    {
        perror("fork");
        return 1;
    }
    else if(id == 0)
    {
        // child
        while(1)
        {
            sleep(1);
            printf("我是一个子进程~, 我的pid: %d, 我的父进程id: %d\n", getpid(), getppid());
        }
    }
    else
    {
        // father
        while(1)
        {
            sleep(1);
            printf("进程开始运行,pid=%d\n", getpid());
        }
    }
    printf("进程开始运行,pid=%d\n", getpid());
    
    return 0;
}

[zjh@izj6cd3zr5adf6jnm3mwihz lesson12]$ ./myprocess
父进程开始运行,pid=13660
进程开始运行,pid=13660
我是一个子进程~, 我的pid: 13661, 我的父进程id: 13660
进程开始运行,pid=13660
我是一个子进程~, 我的pid: 13661, 我的父进程id: 13660
进程开始运行,pid=13660
我是一个子进程~, 我的pid: 13661, 我的父进程id: 13660
我是一个子进程~, 我的pid: 13661, 我的父进程id: 13660
进程开始运行,pid=13660
进程开始运行,pid=13660
我是一个子进程~, 我的pid: 13661, 我的父进程id: 13660
我是一个子进程~, 我的pid: 13661, 我的父进程id: 13660
进程开始运行,pid=13660
我是一个子进程~, 我的pid: 13661, 我的父进程id: 13660
进程开始运行,pid=13660
我是一个子进程~, 我的pid: 13661, 我的父进程id: 13660
进程开始运行,pid=13660
我是一个子进程~, 我的pid: 13661, 我的父进程id: 13660
进程开始运行,pid=13660
```
`fork()` 之后，父子代码默认是共享的（写实拷贝），只是执行不同的代码块，父进程 `id=1`，子进程 `id=0`

为什么fork给父子返回各自不同的返回值？
在系统里父进程与子进程的比例为 `父 : 子 = 1 : n`，因为父进程有多个子进程，需要不同的id来区分子进程，方便对子进程做管理。

 为什么一个函数会返回两次？
 类似于 `STL` 中的 `find()函数`，里面的返回下标，核心工作虽然做完了，但外层还需要进行返回对应下标的数据。`fork函数` 执行完了 `返回id`，`fork函数` 执行完了继续返回到当前代码下一行，`fork()函数` 中申请新的 `pcb`、拷贝父进程 `pcb` 给子进程、`子进程pcb` 放入 `进程list`、在此子进程已经被创建，甚至被调度。所以实际上，在 `return` 之前，父子就已经被返回了（return 也是一条语句）

## 进程状态

### 准备前提
![650](图片/进程状态1.png)![717](图片/进程状态.png)

如图二所示
表面：每一个PCB都被链表维护着，都指向自己的代码数据
深层：一个CPU都要维护一个调度队列（struct struct* runqueue），以先进先出的方式

Linux维护方式：理解内核链表（后面补）
### 运行和阻塞变化过程

- 运行：进程在**调度队列**中，进程状态都是running
- 阻塞：**硬件队列**中，等待某种硬件或资源就绪（键盘，显示器，网卡，磁盘...）
- 硬件队列：OS要管理系统中的各个硬件资源，每个硬件都有信息，并且需要用链表组织起来，方便对硬件状态进行管理

> - 运行 -> 阻塞：当输入 scanf函数 时，此时程序就停止下来
> - 阻塞 -> 运行：本质上就是找到 PCB，再把 PCB 链接到运行队列
>
> 进程状态的变化，表现之一，就是要在不同的队列中进行流动。本质都是数据结构的增删查改！

### 挂起状态
当内存不足时，操作系统会做一系列的交换算法，把相关的内存块（阻塞状态且没就绪的进程）交换到磁盘上，只将 PCB 保留下来，这样就可以节省空间。这种状态称为挂起状态。