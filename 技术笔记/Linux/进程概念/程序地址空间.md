虚拟地址关联到的知识点：硬件、操作系统、编译器、可执行文件、动静态库、线程、进程通信、进程信号
## 程序地址空间

![](图片/程序地址空间.png)

我们用代码来查看一下
```c
#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int g_unval;

int g_val = 100;
int main(int argc, char *argv[], char *env[])
{
    const char *str = "helloworld";
    printf("code addr: %p\n", main);
    printf("init global addr: %p\n", &g_val);
    printf("uninit global addr: %p\n", &g_unval);
    
    static int test = 10;
    char *heap_mem = (char*)malloc(10);
    char *heap_mem1 = (char*)malloc(10);
    char *heap_mem2 = (char*)malloc(10);
    
    char *heap_mem3 = (char*)malloc(10);
    printf("heap addr: %p\n", heap_mem); //heap_mem(0), &heap_mem(1)
    printf("heap addr: %p\n", heap_mem1); //heap_mem(0), &heap_mem(1)
    printf("heap addr: %p\n", heap_mem2); //heap_mem(0), &heap_mem(1)
    printf("heap addr: %p\n", heap_mem3); //heap_mem(0), &heap_mem(1)
    
    printf("test static addr: %p\n", &test); //heap_mem(0), &heap_mem(1)
    printf("stack addr: %p\n", &heap_mem); //heap_mem(0), &heap_mem(1)
    printf("stack addr: %p\n", &heap_mem1); //heap_mem(0), &heap_mem(1)
    printf("stack addr: %p\n", &heap_mem2); //heap_mem(0), &heap_mem(1)
    printf("stack addr: %p\n", &heap_mem3); //heap_mem(0), &heap_mem(1)
    
    printf("read only string addr: %p\n", str);
    for(int i = 0 ;i < argc; i++)
    {
        printf("argv[%d]: %p\n", i, argv[i]);
    }

    for(int i = 0; env[i]; i++)
    {
        printf("env[%d]: %p\n", i, env[i]);
    }

    return 0;
}
```

输出
```
code addr: 0x40057d
init global addr: 0x60103c
uninit global addr: 0x601048
heap addr: 0xf4a010
heap addr: 0xf4a030
heap addr: 0xf4a050
heap addr: 0xf4a070
test static addr: 0x601040
stack addr: 0x7ffe214845d8
stack addr: 0x7ffe214845d0
stack addr: 0x7ffe214845c8
stack addr: 0x7ffe214845c0
read only string addr: 0x400820
argv[0]: 0x7ffe2148580b
env[0]: 0x7ffe21485812
env[1]: 0x7ffe21485826
env[2]: 0x7ffe21485847
env[3]: 0x7ffe21485852
env[4]: 0x7ffe21485862
env[5]: 0x7ffe21485870
env[6]: 0x7ffe21485892
env[7]: 0x7ffe214858a5
env[8]: 0x7ffe214858ae
env[9]: 0x7ffe214858f1
env[10]: 0x7ffe21485e8d
env[11]: 0x7ffe21485ea6
env[12]: 0x7ffe21485f00
env[13]: 0x7ffe21485f0e
env[14]: 0x7ffe21485f1f
env[15]: 0x7ffe21485f36
env[16]: 0x7ffe21485f3e
env[17]: 0x7ffe21485f4d
env[18]: 0x7ffe21485f59
env[19]: 0x7ffe21485f8c
env[20]: 0x7ffe21485faf
env[21]: 0x7ffe21485fce
env[22]: 0x7ffe21485fd7

```

## 虚拟地址

为什么父进程于子进程地址是一模一样的，首先可以理解子进程是对父进程进行了浅拷贝，父子并没有对变量进行任何修改。来一段代码进行演示：
```c
#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

int g_val = 0;

int main()
{
    id_t id = fork();
    if(id < 0){
        perror("fork");
        return 0;
    }
    else if(id == 0){ //child
        printf("child[%d]: %d : %p\n", getpid(), g_val, &g_val);
    }else{ //parent
        printf("parent[%d]: %d : %p\n", getpid(), g_val, &g_val);
    }
    sleep(1);
    return 0;
}
```

```
//与环境相关，观察现象即可
parent[2995]: 0 : 0x80497d8
child[2996]: 0 : 0x80497d8
```

如果将代码进行改动，我们发现，父子进程输出的地址是一样的，但变量内容不一样：
- 变量内容不一样，所以父子进程输出的变量绝对不是同一个变量
- 地址一样，绝对不是物理地址（在 Linux 地址下，叫做虚拟地址）
- 我们在用 C/C++ 所看到的地址，全部都是虚拟地址。用户看不到物理地址，统一由操作系统管理
操作系统必须将 **虚拟地址** 转化为 **物理地址**

## 进程地址空间

更详细的说，程序地址空间应当改为**进程地址空间**，如下图是一个进程的地址空间
![](图片/进程地址空间.png)

从图中总结到的信息：
- 父子进程各有自己的 地址空间（mm_struct）和 页表
- 页表（利用哈希映射）
	- 父进程：左为虚拟地址，右为物理内存
	- 子进程：左为物理内存，右为虚拟地址
	- 虚拟地址和物理内存可以互相访问
- 父进程的页表右指向物理内存，子进程的页表左指向物理内存

1. 不同的操作系统下，地址空间的内存也不一样：如 32位操作系统 内存为4g，64位操作系统 内存为 32g。
2. 父进程 创建 若干个子进程，子进程对父进程的虚拟地址和页表进行浅拷贝，为什么子进程修改内容时，父进程却没有改变？
	- 子进程修改内容时，原本指向同一块父进程的物理地址 因 操作系统拷贝了一份物理地址给了子进程，让子进程页表左边 指向 拷贝的物理地址，这就是为什么父子进程返回的变量不相同
	- 由于父子进程的虚拟地址没有被改指向，子进程浅拷贝了父进程的页表，却没有改虚拟地址，所以返回的虚拟地址是相同的
	- 由于父子进程的内容不一样，所以返回的内容也不一样。即一个变量，即 == 0 又 > 0，导致 if else 进入不同类域中

## 为什么要有地址空间

用户不能直接访问物理内存，单可访问虚拟内存，当物理内存占 100 字节是，在虚拟内存空间开辟 100 字节，通过页表映射，就可访问物理内存

虚拟地址空间是一个结构体（每一个区域的起始内存和终止内存），不是内存

1. 将 addr 从无序变有序（物理内存随便加载到内存，单通过页表的映射关系，从无序变为有序）
2. 虚拟地址 -> 物理地址（通过操作系统查找页表）：转化过程种，也可以对你的地址和操作进行合法判定
	- 野指针：查页表找不到物理内存，操作系统发现转化失败，野指针后把进程杀死
	- 页表权限：`char* str = "hello"; str = "x"`，修改 str 程序崩溃，愿望查找页表，权限拦截
3. 进程管理和内存管理，进行一定程度的解耦合

## 虚拟内存管理

描述 Linux 下进程的地址空间的所有的信息的结构体是 mm_struct。每一个进程只有一个 mm_struct 结构，在每一个进程的 task_struct 结构中，有一个指向该进程的 mm_struct 结构体指针
```c
struct task_struct
{
	struct mm_struct *mm; // 对普通用户来说，该字段指向虚拟地址空间的用户空间部分，对内核线程这部分为NULL
	struct mm_struct *active_mm; // 内核线程使用。如果该进程是内核线程，它的 mm 字段为 NULL，表示没有内存地址空间，也不一定，这是因为所有进程关于内核的映射都是一样的，内核线程可以使用任意进程的地址空间
	
	/*...*/
}
```

可以说，`mm_struct` 结构是对整个用户的描述。每一个进程都会有自己独立的 `mm_struct`，这样每一个进程都会有自己独立的地址空间才能互不干扰。`task_struct` 到 `ss_struct` 进程的地址空间的分布情况：
![](图片/task_struct到mm_struct分布情况.png)

每一个进程都有自己的 `mm_struct`，操作系统会将多个进程的 `mm_struct` 组织起来，组织成链表，方便对虚拟地址的增删查改。虚拟空间的组织方式有两种：
- 当虚拟区较少，采取单链表，有 mmap 指针指向这个链表
- 当虚拟区多是采取红黑树进行管理，有 mm_rb 指向这棵树

`Linux` 内核使用 `vm_area_struct` 结构来表示一个独立的虚拟内存区域（VMA），由于每一个不同质的虚拟内存区域功能和内部机制都不同，因此一个进程使用多个 `vm_area_struct` 结构来分别表示不同类型的虚拟内存区域。上面的两种组织方式就是使用 `vm_area_struct` 结构来连接各个 VMA，方便进程快速访问。
```c
struct vm_area_struct {
	unsigned long vm_start; //虚存区起始
	unsigned long vm_end; //虚存区结束
	struct vm_area_struct *vm_next, *vm_prev; //前后指针
	struct rb_node vm_rb; //红⿊树中的位置
	unsigned long rb_subtree_gap;
	struct mm_struct *vm_mm; //所属的 mm_struct
	pgprot_t vm_page_prot;
	unsigned long vm_flags; //标志位
	struct {
	struct rb_node rb;
	unsigned long rb_subtree_last;
	} shared;
	struct list_head anon_vma_chain;
	struct anon_vma *anon_vma;
	const struct vm_operations_struct *vm_ops; //vma对应的实际操作
	unsigned long vm_pgoff; //⽂件映射偏移量
	struct file * vm_file; //映射的⽂件
	void * vm_private_data; //私有数据
	atomic_long_t swap_readahead_info;

#ifndef CONFIG_MMU
	struct vm_region *vm_region; /* NOMMU mapping region */
#endif
#ifdef CONFIG_NUMA
	struct mempolicy *vm_policy; /* NUMA policy for the VMA */
#endif
	struct vm_userfaultfd_ctx vm_userfaultfd_ctx;
} __randomize_layout;
```

上述代码如下图所示：
![717](图片/vm_area_struct%201.png)![](图片/vm_area_struct%202.png)