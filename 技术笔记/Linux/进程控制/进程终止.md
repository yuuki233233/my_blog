# 进程终止

进程终止的本质就是释放系统资源，释放进程中申请的相关内核数据结构和对应的数据和代码

## 进程退出场景

进程退出无非三种场景：
1. 代码运行完毕，结果正确
2. 代码运行完毕，结果不正确
3. 代码异常终止
父进程要获取到子进程的信息，也就是进程退出场景的结果

## 进场常见退出方法

正常终止（可通过 `echo $?` 查看进程退出码）：
1. 从 `main` 返回
2. 调用 `exit`
3. 调用 `_exit`
异常退出
- `Ctrl + c`
- 信号终止

### 退出码

退出码可查看最后一次执行命令的状态。程序返回退出码 0 时，表示执行成功。代码返回 1 或 0 以外的任何退出码，都被视为不成功

```bash
[zjh@izj6cd3zr5adf6jnm3mwihz lesson16]$ cat proc.c
#include <stdio.h>
#include <string.h>

int main()
{
    FILE* f = fopen("log.txt", "r");
    if(f == 0) return 1;

    return 0;
}

[zjh@izj6cd3zr5adf6jnm3mwihz lesson16]$ ./proc

[zjh@izj6cd3zr5adf6jnm3mwihz lesson16]$ echo $?
1
```
非正常退出，打印 return 1

也可以查看命令的进程码：
```bash
[zjh@izj6cd3zr5adf6jnm3mwihz lesson16]$ ls v.cpp
ls: cannot access v.cpp: No such file or directory

[zjh@izj6cd3zr5adf6jnm3mwihz lesson16]$ echo $?
2

[zjh@izj6cd3zr5adf6jnm3mwihz lesson16]$ ls v.c
v.c

[zjh@izj6cd3zr5adf6jnm3mwihz lesson16]$ echo $?
0
```
`ls` 没有找到时，进程码返回 2，`ls` 找到时，进程码返回 0

机器返回错误结果都是数字，我们人类想查看返回错误结果，就需要看字符串，对此 C标准库 里的提供了错误信息的字符串
 ```bash
 [zjh@izj6cd3zr5adf6jnm3mwihz lesson16]$ cat proc.c
#include <stdio.h>
#include <string.h>

int main()
{
    int i = 0;
    for(; i < 200; i++)
    {
        printf("%d -> %s\n", i, strerror(i));
    }
   
    //FILE* f = fopen("log.txt", "r");
    //if(f == 0) return 1;

    return 0;
}
 ```
进程一旦出现异常，一般是进程收到了信号

Linux Shell 中的主要退出码：

| 退出码   | 解释                                        |
| ----- | ----------------------------------------- |
| 0     | 命令执行成功                                    |
| 1     | 通用错误代码                                    |
| 2     | 命令或参数使用不当                                 |
| 126   | 权限被拒绝或无法执行                                |
| 127   | 未找到命令或PATH错误                              |
| 128+n | 命令被信号从外部终止或遇到致命错误                         |
| 130   | 通过 `Ctrl+c` 或 `SIGINT` 终止（终止代码 2 或 键盘中断）  |
| 143   | 通过 `SIGTERM` 终止（默认终止）                     |
| 255/* | 退出码超过了` 0-255` 的范围，因此重新计算（超过 255 后，用退出取模） |
- 0：表示命令执行无误
- 1：不被允许操作。如在没有 sudo 权限的情况下
- 130：`SIGINT` 或 `^C` 和 `143`（`SIGTERM`）等终止信号时非常典型的，它们属于 `128+n` 信号，其中 `n` 代表终止码
- 可以使用 `strerror` 函数来获取退出码对应的描述

### main函数退出

main函数的返回值，通常表明你的程序的执行情况
正常终止：
1. `return 0;`（代码运行完毕，结果正确）
2. `return !0;`（代码运行完毕，结果不正确，不同的返回值表明不同的出错原因）
代码异常，返回值无意义

### `_exit`函数
```c
#include <unistd.h>
void _exit(int status);
```
- 参数：status 定义了进程的终止状态，父进程通过 wait 来获取该值

### exit函数
```c
#include <unistd.h>
void exit(int status);
```
exit 最后也会调用 `_exit`，但在调用 `_exit` 之前，还做了其他工作：
1. 执行用户通过 atexit 或 on_exit 定义的清理函数
2. 关闭所有打开的流，所有的缓存数据均被写入
3. 调用 `_exit`

![471](图片/执行exit过程.png)
区别：
- 进程如果 exit 退出的时候，`exit()`，进程退出的时候，会进行缓冲区的刷新
- 进程如果 exit 退出的时候，`_exit()`，进程退出的时候，不会进行缓冲区的额刷新